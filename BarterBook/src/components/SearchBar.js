import React from 'react'

const SearchBar = (props) => {
    return (
        <div>

            <form>
                <input placeholder="I'm a Search Form!"></input>
            </form>
        </div>
    )
}

export default SearchBar